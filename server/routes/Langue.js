/*const express = require("express");
const router = express.Router();
const langue=require("../models/SejourLangue")

router.post('/',async=(req,res)=>{
    const newLangue= new langue({...req.body})
try {
   await newLangue.save()    
} catch (error) {
    res.status('404').json({msg:'imm'})
}
})*/
